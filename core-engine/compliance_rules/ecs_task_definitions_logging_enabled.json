{
  "Compliance Name": "aws_foundational_security_best_practices_aws",
  "ID": "ECS.1",
  "Name": "Amazon ECS task definitions should have secure networking modes and user definitions",
  "Description": "This control checks whether an Amazon ECS task definition with host networking mode has 'privileged' or 'user' container definitions.",
  "Function Name": "ecs_task_definitions_logging_enabled",
  "clients": ["ecs"],
  "functions": {
    "ecs": {
      "method": "describe_task_definition",
      "field_path": "taskDefinition.containerDefinitions[].logConfiguration"
    }
  },
  "evaluation": {
    "type": "exists",
    "source_client": "ecs",
    "field": "logConfiguration"
  }
}
