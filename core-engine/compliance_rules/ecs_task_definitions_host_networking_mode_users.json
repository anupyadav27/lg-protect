{
  "Compliance Name": "pci_4.0_aws",
  "ID": "7.2.3.4",
  "Name": "ECS Task Definitions Host Networking Mode Users",
  "Description": "Checks if ECSTaskDefinitions specify a user for Amazon Elastic Container Service (Amazon ECS) EC2 launch type containers to run on",
  "Function Name": "ecs_task_definitions_host_networking_mode_users",
  "clients": ["ecs"],
  "functions": {
    "ecs": {
      "method": "describe_task_definition",
      "field_path": "taskDefinition.containerDefinitions[].user"
    }
  },
  "evaluation": {
    "type": "exists",
    "source_client": "ecs",
    "field": "user"
  }
}
